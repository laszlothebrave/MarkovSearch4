#include "pch.h"
#include "DerivativeNormPolynomial.h"

DerivativeNormPolynomial::DerivativeNormPolynomial(const double& a, const double& b, const double& c, const double& d, const double& e, const double& f) : a(a), b(b), c(c), d(d), e(e), f(f)
{
	
}
